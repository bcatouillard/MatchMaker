{% extends 'base.html.twig' %}

{% block title %} {{ parent()}} - Modifier match
{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="title">Modifier le match</h1>

		{{ include('match_maker/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

		<a href="{{ path('match_maker_index') }}">Retour à la liste</a>

		{{ include('match_maker/_delete_form.html.twig') }}
	</div>
{% endblock %}
